# Legacy Architecture Record

> Legacy document retained for historical context only. Do not use it as implementation guidance. The current source of truth is `docs/ARCHITECTURE_V2.md`.

The original blueprint described an exploratory Astro/MDX portfolio with a generic component and content split. It predates the current Content Layer schema, GitHub Pages base-path handling, Project Registry route and automated QA gates.

Current replacements:

- Read `docs/ARCHITECTURE_V2.md` for runtime boundaries and routes.
- Read `docs/CONTENT_GUIDE.md` for the MDX contract.
- Read `docs/ROADMAP.md` for release work.
- Read `docs/plan/README.md` for test status and CI behavior.

This legacy record must not be used to introduce removed dashboard concepts, unverified metrics or components that are not present in the current roadmap.
