name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Safe dependency patch update check
        run: npm outdated --long || true
      - name: Audit (report only)
        run: |
          npm audit --audit-level=moderate || echo "::warning:: audit has vulnerabilities, see report (Astro 7 evaluation is a separate phase)"
        continue-on-error: true
      - name: L1 - Content schema validation
        run: npm run test:unit
      - name: Type check
        run: npm run check
      - name: Build
        run: npm run build
      - name: Assert generated page count
        run: |
          page_count=$(find dist -type f -name 'index.html' | wc -l | tr -d ' ')
          test "$page_count" -eq 7
      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium
      - name: L2 - Smoke tests
        run: npm run test:smoke
      - name: L3 - Visual regression tests
        run: npm run test:visual
      - name: Verify sitemap & robots
        run: |
          ls -lh dist/sitemap-index.xml dist/sitemap-0.xml dist/robots.txt
          test -f dist/sitemap-index.xml
          test -f dist/robots.txt
      - name: Upload Playwright artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-artifacts
          path: test-results/
          if-no-files-found: ignore
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "dist"

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
